<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Tema de arranque de la actividad. Solo define el fondo mientras Compose toma el control:
        si fuera claro, se veria un destello blanco al abrir el launcher sobre un fondo negro.
        Las barras del sistema van transparentes porque la UI se dibuja borde a borde.
    -->
    <style name="Theme.SyncroLauncher" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowShowWallpaper">false</item>
    </style>
</resources>
